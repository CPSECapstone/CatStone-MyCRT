matrix:
   include:
      - language: python
        python: 3.2
        install:
           - pip install flask flask_restful flask_cors flask_jsonpify flask_script flask_migrate
        script:
           - cd mycrt-backend/test
           - python test.py
